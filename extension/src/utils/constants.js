// API endpoint
export const API_URL = 'http://localhost:3000/api';

// Fallback API endpoint if localhost doesn't work
export const FALLBACK_API_URL = 'http://127.0.0.1:3000/api';

// Key for storing encrypted data in Chrome storage
export const STORAGE_KEY = 'pwm_encrypted_data';

// Encryption key storage key
export const ENCRYPTION_KEY = 'pwm_encryption_key';

// Sync interval in milliseconds (5 minutes)
export const SYNC_INTERVAL = 5 * 60 * 1000;
